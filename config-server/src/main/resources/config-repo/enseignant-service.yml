# Configuration Enseignant Service - Profil par défaut

server:
  port: 8082

spring:
  application:
    name: enseignant-service

  datasource:
    url: jdbc:mysql://localhost:3306/daos_enseignant_db?createDatabaseIfNotExist=true
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# Swagger Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  packages-to-scan: com.uasz.daos.enseignant.controller,com.uasz.daos.enseignant.api

# Logging
logging:
  level:
    com.uasz.daos.enseignant: DEBUG
    org.springframework.web: INFO
