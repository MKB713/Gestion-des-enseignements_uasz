# Ce fichier peut rester vide ou contenir uniquement des valeurs par défaut
# Toutes les configurations sont maintenant dans le Config Server
# Feign Logging Configuration
logging.level.com.uasz.daos.choix.client=DEBUG
feign.client.config.default.loggerLevel=FULL

# Timeout Configuration (optionnel)
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=5000
